import React from 'react';
import DuesManagement from './DuesManagement';

// React Query version of DuesManagement - currently wraps the existing component
const DueManagementReactQuery: React.FC = () => {
  return <DuesManagement />;
};

export default DueManagementReactQuery;
